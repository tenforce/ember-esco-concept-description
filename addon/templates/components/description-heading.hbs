{{#if items}}
  {{#if title}}
    {{description-item-abstract model=title concept=concept emptyTitle="emptyItem"}}
  {{/if}}
  {{description-items model=items concept=concept emptyItems="emptyItems"}}
{{/if}}