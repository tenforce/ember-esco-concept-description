<div class='action'>
  <button type="button" class='add' id= {{buttonId}} title= {{buttonTitle}} {{action 'togglePopup'}}>+</button>

  {{#if displayPopup}}
      {{popup-content elements=elements conceptScheme=conceptScheme togglePopup=(action 'togglePopup')}}
  {{/if}}
</div>
