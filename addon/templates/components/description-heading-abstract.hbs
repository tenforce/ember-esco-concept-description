{{#if displayAlternate}}
  {{description-heading model=model concept=concept title=title items=items emptyHeading="emptyHeading"
  tagName=getTagName classNames=getClassNames}}
{{else}}
  {{description-heading model=model concept=concept title=title items=items emptyHeading="emptyHeading" tagName=getTagName classNames=getClassNames}}
{{/if}}